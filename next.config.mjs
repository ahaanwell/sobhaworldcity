// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   /* config options here */
// };

// export default nextConfig;


const nextConfig = {
  output: "export", // 👈 important
  trailingSlash: false,
};

export default nextConfig;